/*
 * Filename: prog1.c
 * Author: Junru Ren
 * Description: C program for learning array, logics and controls in C
 * Date: Jan 18 2017
 */

#include <stdio.h>
#include <stdlib.h>

int main()
{
  // Arrays
  // Almost all the computer programs need to deal with big chunks of data
  // Array is a built-in data structure in C (also in almost all other languages)
  // It looks like this:
  int[] arr = {8, 7, 2, 3, 5};
  // Above is an array of 5 integers; each element is stored next to each others
  // in the memory
  return EXIT_SUCCESS;
}
